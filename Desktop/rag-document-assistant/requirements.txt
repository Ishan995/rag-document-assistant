# Web server & API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9

# UI
streamlit>=1.40.0
httpx>=0.27.0

# LangChain & local LLM / vector store
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-ollama>=0.2.0
langchain-text-splitters>=0.3.0

# Embeddings & vector store
faiss-cpu>=1.9.0
sentence-transformers>=3.0.0
pypdf>=5.0.0

# Data / validation (FastAPI & LangChain)
pydantic>=2.0.0

# PyTorch (required by sentence-transformers; pin optional per your CUDA/CPU needs)
torch>=2.0.0

# Hugging Face stack (used by sentence-transformers)
transformers>=4.40.0
huggingface-hub>=0.20.0
safetensors>=0.4.0

# Numerics (faiss / ML stack)
numpy>=1.24.0
